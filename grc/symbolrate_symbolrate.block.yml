# auto-generated by grc.converter

id: symbolrate_symbolrate
label: Symbolrate
category: '[SYMBOLRATE]'

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: float
    default: samp_rate
-   id: valid_min
    label: Valid Min
    dtype: float
    default: '0.0'
-   id: valid_max
    label: Valid Max
    dtype: float
    default: '0.0'
-   id: log_rates
    label: Log Rates
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: float

outputs:
-   label: inst_rate
    domain: stream
    dtype: float
    optional: true
-   label: short_avg
    domain: stream
    dtype: float
    optional: true
-   label: full_avg
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import symbolrate
    make: symbolrate.symbolrate(${samp_rate},${valid_min},${valid_max},${log_rates})

file_format: 1
